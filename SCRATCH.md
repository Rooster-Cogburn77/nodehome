# Session Scratch - 2026-05-15 (Session 23)
Focus: Re-frame Nodechat scope as full agentic terminal environment, land Observe-tier auto-routing lanes, close the apply loop with `/undo-apply`, add live-node operator paths, and implement model routing through env-gated remote profiles.

## What was decided/executed this session
- **2026-05-18 house power-blip check logged.** After the blip, remote `./scripts/healthcheck.sh` returned `[HEALTHY] no failures, no warnings`. `upsc ups` showed `battery.charge: 99`, `input.voltage: 120.0`, `ups.status: OL CHRG`, `ups.load: 0`, and `ups.realpower.nominal: 900`. Useful conclusion: node health was clean after the event and UPS telemetry was normal, but because `ups.load: 0` and rack/node AC are not inline through UPS battery outlets, this did **not** validate backup-power behavior for the node.
- **BMC VBAT sensor follow-up closed with battery install.** BMC Sensor Readings over the USB-NIC tunnel initially showed `VBAT` = `Battery Failed`; host confirmation matched (`sudo ipmitool sensor get VBAT` showed `States Asserted: Battery [Failed]`, and SDR showed `VBAT ... Failed`). A CR2032 3V coin cell was installed 2026-05-18. Post-install `sudo ipmitool sensor get VBAT` reports `[Presence Detected]`, and SDR reports `VBAT | ... | Presence Detected`. Battery failure is cleared; retention still needs proof on the next full power-loss check.
- **BMC SSL page inspected.** `Configuration -> Network -> SSL Certificates` shows current cert validity `Sep 4 2024 GMT` to `Sep 4 2034 GMT` and upload controls for `New SSL Certificate` plus `New Private Key` (`.pem`/`.cert`). No CSR/self-signed generator was visible on that page. Durable cert hygiene likely requires generating an RSA cert/key outside the BMC for the final BMC hostname/static IP, then uploading both after the network target is chosen.
- **BMC port/access posture captured.** `Configuration -> Network -> Port` shows IKVM TCP `5900`, SSH TCP `22`, Web TCP `80`, Web SSL TCP `443`, Virtual Media TCP `623`, and IPMI LAN UDP `623` enabled; SNMP UDP `161` is disabled. `Configuration -> Network -> IP Access Control` is currently `OFF`.
- **BMC remaining settings inventory captured.** `Configuration -> Network -> SSDP` was initially `ON` at UDP `1900`, TTL `2`, IPv6 scope `Link`; user then disabled SSDP in the same BMC sitting. `LLDP` is `OFF` and transmit is `OFF`. `Configuration -> BMC Settings`: Dynamic DNS is `OFF` and warns that NTP sync is required before hostname changes; SMC RAKP is `OFF`; KCS Control is `Administrator`; IPMI Configuration exposes config `Download` and reload-from-file only; Host Interface is `ON` with host `169.254.3.1`, service `169.254.3.254`, USB mode `RNDIS Connection`; System Lockdown is license-gated behind `SFT-DCMS-SINGLE`; Web Session timeout is `30` minutes. Leave KCS/Host Interface alone until dedicated BMC network access is proven.
- **BMC SEL clock corrected manually.** Host time was synchronized (`host_utc=2026-05-18T03:06:39Z`, `timedatectl` NTP active). BMC was stale at `04/01/2025 01:44:20 AM UTC`. No host UDP `:123` listener was present on the USB-NIC path, so BMC NTP was not enabled. `sudo ipmitool sel time set "$(date -u '+%m/%d/%Y %H:%M:%S')"` failed with `Specified time could not be parsed`; `sudo ipmitool sel time set now` succeeded and `sudo ipmitool sel time get` returned `05/18/2026 03:06:39 AM UTC`. During the battery replacement power cycle, BMC time came back as `03/28/2025 12:41:09 PM UTC`; after the new CR2032 was installed and VBAT reported presence, `sudo ipmitool sel time set now` succeeded again at `host_utc=2026-05-18T03:40:28Z` and `sudo ipmitool sel time get` returned `05/18/2026 03:40:28 AM UTC`. Clock is sane while powered; retention needs proof on the next full power-loss check.
- **Homelab clone synced after history rewrite.** On `homelab`, `cd ~/nodehome && git fetch origin && git reset --hard origin/main` moved the local checkout to `b4d8575 Record BMC battery replacement`; `git status --short --branch` returned `## main...origin/main`. Immediate BMC time spot-check showed `sudo ipmitool sel time get` = `05/18/2026 03:44:02 AM UTC` and host UTC = `2026-05-18T03:44:03Z`, so the BMC SEL clock was tracking the host within one second while powered after the CR2032 install.
- **Nodechat one-shot `/live journal ollama` re-smoked on homelab.** After `git pull --ff-only`, the node clone fast-forwarded to `7a77d2b`. `python3 scripts/nodechat.py --once "/live journal ollama"` started session `20260518-050029`, ran `journalctl -u ollama --no-pager -n 200` locally, returned `exit_code: 0`, preserved the newest tail under truncation, and printed `live context added: journal ollama`. Journal evidence showed older May 17 GPU OOM/backoff/CPU-fallback behavior and runner kills under large-context `gemma3` loads; after the May 18 reboot, Ollama `0.23.2` started cleanly and detected both visible 3090s with ~23 GiB free each. Remaining issue is model-load pressure under high context / constrained VRAM, not Nodechat's journal path.
- **Ollama/vLLM coexistence note added.** New runbook `docs/runbooks/ollama-vllm-coexistence.md` captures the operator rule from the journal evidence: Ollama service/API health is separate from model-load capacity, and CUDA OOM/backoff/CPU fallback under resident vLLM memory pressure should be treated as expected capacity contention unless service health also fails. Use Open WebUI's `vllm.*` path while vLLM is resident; intentionally stop vLLM only for an Ollama-heavy task.
- **Sweep digest quality hardening added.** The 2026-05-17 extended and 2026-05-18 core digest emails exposed three real quality failures: social-primary rows dominated Top Signals, non-actionable `Validation: direct-post` lines repeated, and Simon Willison GitHub activity / consecutive `llama.cpp` releases spammed the digest. `sweeps/run_daily.py` now down-ranks social-primary rows unless follow-up evidence exists, suppresses direct-post validation noise, collapses repeated GitHub activity by action/repo/date, drops low-value branch/star/comment-only GitHub activity, and dedupes Top Signals / synthesis candidate selection for release series. Regression tests added in `tests/test_sweeps_digest_quality.py`.
- **Remaining sweep triage items addressed.** The vLLM blog source now uses `https://vllm.ai/blog` and article-card parsing, so it emits real post titles instead of `Blog | vLLM`. Heuristic summaries no longer add generic opener/filler sentences and the LLM synthesis prompt explicitly bans the known stock phrases. Consumer-gaming hardware rows are filtered unless they include real stack-relevant hardware context, and power/network topic matching now uses word/phrase guards to reduce partial-keyword false positives.
- **X/OpenRSS made explicit opt-in as mitigation only.** Local health showed core X bridge sources repeatedly retrying after quarantine cooldown and falling back to stale cached state, while extended X bridge sources sat in long quarantined lists with OpenRSS 403/429/timeouts. `x_user` sources now skip by default unless `X_BEARER_TOKEN` exists or `SWEEP_OPENRSS_FALLBACK_ENABLED=true` is explicitly set. Skipped X transport clears stale degraded counters but still surfaces as non-ok in `sweeps/report_status.py`; reliable X social collection remains open in `sweeps/PIPELINE_FOLLOWUPS.md`.
- **X email notification ingest path selected, not yet proven.** `xnotify@nodehome.ai` on Forward Email is the chosen inbox for restoring X social signal without relying on OpenRSS. Repo plumbing already exists: `sweeps/ingest_x_email.py`, `x-email-notifications` in `sweeps/sources.json`, and workflow pre-ingest when `X_EMAIL_IMAP_*` env is configured. User began adding/enabling tracked X accounts and hit an X follow/notification rate limit. The prior tracked list is enabled except `@concat_ai`, which user identified as a scam account and removed from tracking. Added new extended tracked handles: `@KyleHessling1`, `@UnslothAI`, `@stingning`, and `@AndrewYNg`. No IMAP dry-run, JSONL proof, or no-send sweep proof yet; continue after cooldown.
- **ISP router capability check completed.** Local default gateway `192.168.1.1` is a Spectrum/Sagemcom WiFi 6 router in the `SAX1V1S` / Fast 5285 class; the router info page exposed model/firmware, but the public WAN IP and serial were intentionally not recorded in repo. User physically confirmed the house has a separate Spectrum modem feeding the Spectrum router over ethernet. Current Spectrum guidance for separate modem/router service is to connect an owned router straight to the Spectrum modem instead of their router; the SAX1V1S documentation is for an app-managed Advanced WiFi router, not a bridge-mode gateway. Working decision: treat the network replacement path as bypass/return the Spectrum router and put the UniFi Cloud Gateway Ultra WAN directly on the Spectrum modem. Do not build long-term BMC VLAN/security on the SAX1V1S or accept double NAT as the final posture. BMC cert/static-IP remains blocked until the UniFi management subnet and hostname are chosen.
- **Historical Claude attribution trailers scrubbed.** After explicit user direction to scrub it, ran a precise trailer audit using `git interpret-trailers` across all reachable commits: pre-rewrite state was `174` commits with `58` commits carrying `Co-Authored-By: Claude...` trailers. `git-filter-repo` dry run on a fresh mirror clone preserved `174` commits and reduced trailer hits to `0`; the real repo rewrite then produced initial post-rewrite HEAD `664bdd6` with `174` commits and `0` trailer hits before this follow-up log commit. External safety bundle: `C:\tmp\nodehome-pre-claude-trailer-scrub-20260517-212141.bundle`.
- **Full-stack inventory durably captured.** New `docs/wiki/concepts/full-stack-inventory.md` is the cross-cutting roll-up of every component (hardware, OS, inference, custom tooling, media stack, backup, security, newsletter, watch lane, validation backlog) with status (shipped / in-flight / planned / deferred / watch / declined) and the trigger that would flip a status. Companion-doc links point back to the deeper runbooks per area. `docs/CURRENT_STATE.md` now links to it at the top so it's discoverable. Maintenance rule documented in the file itself: shipped/in-flight/planned/deferred transitions update the rows; bump the "Last reviewed" date on substantive sweeps. This is meant as the pull-from-reliably reference, not a replacement for runbooks.
- **Open WebUI pinned to v0.9.5.** Replaced the rolling `ghcr.io/open-webui/open-webui:main` container with `ghcr.io/open-webui/open-webui:v0.9.5`, preserving the existing named volume and Open WebUI UI-side connections. Verification: container healthy, local HTTP probe returned `OPENWEBUI_HTTP_OK`, browser smoke worked through both `vllm.Qwen/Qwen2.5-32B-Instruct-AWQ` and `gemma3:27b`, and post-migration backup exists at `/home/bmoore_77/open-webui-backups/open-webui-v095-postmigration.tgz` (`979M`). `AIOHTTP_CLIENT_ALLOW_REDIRECTS` remains unset.
- **Manual inbox lifecycle trap fixed.** Today's `--skip-email` extended verification wrote `docs/sweeps/state/manual-stack-articles.json`, so the scheduled email workflow later regenerated the extended digest and omitted the three curated rows as already-seen. The permanent fix changes `manual-stack-articles` semantics from normal diff-state to current-queue rendering: if rows remain in ignored `docs/sweeps/inbox/manual_stack_articles.jsonl`, every `extended` or `all` run renders them even when prior state contains their IDs. Regression test added. Operational rule: clear/edit the manual inbox only after the intended digest has actually been sent.
- **Corrected 2026-05-16 extended digest sent.** Regenerated `docs/sweeps/daily/2026-05-16.extended.md` through the fixed manual-inbox path, verified it included `delta-mem`, `Orthrus`, and `The CTF Scene Is Dead`, then sent the exact artifact with subject `Daily Sweep - 2026-05-16.extended (corrected)`. Sender dry-run resolved `to: digest@nodehome.ai`, `bcc: bmoore7789@gmail.com`; real send returned Resend id `75df228a-2f82-4a96-af26-5e790e8694b5`. Ignored manual inbox was cleared afterward so the three already-sent rows do not repeat in future extended runs.
- **SubQ/Appen SSA update logged as watch-only.** The Appen/SSA benchmark update strengthens SubQ as a hosted long-context architecture watch item: current public claims include 12M-token product positioning, Appen-reported SSA kernel speedups at very long context, and SubQ benchmark tables around RULER 128K / MRCR v2 / SWE-Bench Verified. Current decision is unchanged: no Nodehome serving change, no local 3090 target, and no replacement for Nodechat AI History/RAG. Revisit only if early access opens or public evaluation/model-card/pricing evidence is strong enough to run real repo-history tasks against Nodechat `strong` plus auto-routed context.
- **GPU rental economics checked; no current pivot.** A 3x RTX 3090 rental-host path was sanity-checked against Vast/Clore/Akash-style marketplace economics. Under normal RTX 3090 rates, a 3-card box looks like low-hundreds/month net after platform fees and power, not `$3k/month`; `$3k/month` likely implies a much larger farm, higher-end GPUs, unusually cheap/stranded power, or loose accounting. Current decision: do not turn Nodehome into a rental host. Revisit only after the GPU3 proper cable is installed, 3-GPU thermal/power validation passes, BMC/network isolation is hardened, and fresh marketplace prices justify an idle-monetization experiment. Economics can move quickly, so keep this as a watch item rather than a dead idea.
- **Nodechat paste/interrupt fix live-smoked on homelab.** After pulling `origin/main` to `/home/bmoore_77/nodehome`, a fresh Nodechat session `20260516-175111` started on the vLLM endpoint. The operator ran `say numbers 1 to 100 slowly`, pressed `Ctrl-C` during streaming, and Nodechat printed `CHAT_INTERRUPTED` then returned to `nodechat:175111>` with no Python traceback. Multi-line direct paste remains an operator footgun; use `/paste` and terminate with `.`.
- **UPS comms cable installed and verified; power path not inline.** APC UPS telemetry is now live on `homelab` through USB + NUT. Proof captured 2026-05-16: `lsusb` showed `051d:0002 American Power Conversion Uninterruptible Power Supply`; kernel logs identified `Back-UPS XS 1500M FW:947.d13 .D USB FW:d13`; NUT `upsc ups` returned real fields including `battery.charge: 99`, `input.voltage: 122.0`, `ups.status: OL CHRG`, `driver.name: usbhid-ups`, and `ups.realpower.nominal: 900`; Nodechat `/live ups` then ran `upsc ups` with `exit_code: 0` and added `live context added: ups`. This was a connectivity/telemetry test only: rack/node AC power is not currently routed through the UPS battery-backed outlets, so `ups.load: 0` is expected and runtime estimates are not meaningful. The BX/XS1500M remains useful for telemetry practice, low-power gear, or maybe idle/light-load graceful shutdown, but it is not the production UPS for sustained 2-GPU or 3-GPU inference.
- **Scope correction shipped (commit `938f3cd`).** New authoritative scope doc `docs/runbooks/nodechat-scope.md` defines Nodechat as the local agentic terminal environment for Nodehome — capability with evidence as the philosophy, 5-tier risk model (Observe / Analyze / Prepare / Mutate / Dangerous) as the control system. `nodechat-terminal.md` re-framed; `CURRENT_STATE.md` and `scripts/README.md` link the scope doc.
- **Auto-routing lane #1 implemented and tested.** AI History routes on prior-decision phrasing; repo routes on concrete artifacts only (named files, known runbook stems, path tokens). Per user direction: vague topic phrases skip, bare filenames skip, history query is sent verbatim (no normalization), no `git status` injection on vague repo prompts. Two-file cap on repo routing per turn. Failures never block the chat turn — they emit an `auto_route_*` audit event with `status=error` and disclose inline.
- **Disclosure + context controls + structured provenance + audit landed together.** `[auto-routed: ...]` line above every assistant reply that routed anything. `/history-mode`, `/repo-mode`, `/evidence`, `/forget` commands. Every context block now carries `source` and `provenance`; legacy blocks render under `manual-legacy`. New audit event types: `auto_route_history`, `auto_route_repo`.
- **Undo lane implemented.** `/undo-apply [n|latest] [--check]` restores an applied proposal from its apply-time backup after verifying the current file still matches the exact applied content. It refuses post-apply file drift, writes an undo safety backup, records `undone_at`/`undo_backup_path`, injects `manual-undo-apply` context, and audits check/confirm/refusal paths.
- **Web auto-routing lane implemented.** `/web-mode auto|manual|off` now controls Observe-tier web routing. Direct URLs auto-fetch; fresh-public prompts (latest/release/version/CVE/pricing/availability/market) plus a public object auto-route through DuckDuckGo HTML search. Local-only status phrasing intentionally skips web. Auto web uses the same bounded `/web-search` and `/web-fetch` internals, a short timeout, disclosure, `auto-web-search` / `auto-web-fetch` context sources, and `auto_route_web` audit events.
- **Live-node operator lane implemented.** `/live [all|health|gpu|power|docker|vllm|ollama|storage|bmc|ups|smart /dev/<device>]` runs fixed Observe-tier status checks locally or through optional SSH (`NODECHAT_LIVE_SSH`, default root `~/nodehome`). `/live-mode auto|manual|off` controls auto-routing. Live prompts can inject `LIVE_NODE_STATUS` with commands, exit codes, executable provenance, bounded output, and `auto_route_live` / `live_check_executed` audit events. Selected service restarts are now Mutate-tier approval rows through `/approve`.
- **Grouped evidence view implemented.** `/evidence` now groups active context blocks by `source`, shows source-level counts and char totals, prints reference summaries, and preserves global block indexes for `/forget <n>`.
- **Auto-routing recall pass — Phase A shipped (measurement infrastructure).** `tests/routing_corpus.py` is a 100-prompt labeled corpus + harness covering history, repo, web, live positives plus neutral/general prompts and adversarial guardrails. Harness runs as a CLI and as a regression test class. Phase A baseline (2026-05-15): repo precision/recall = 1.00; history 0.81 / 0.81; web 0.82 / 0.93; live 0.78 / 0.68. Floors pinned at baseline as a regression ratchet; six guardrails recorded as `PHASE_B_GUARDRAIL_TARGETS` with an inverse ratchet.
- **Auto-routing recall pass — Phase B history landed.** Patterns split into TIGHT (project-bound by phrasing) and BROAD (require co-occurrence with new `HISTORY_PROJECT_CONTEXT_RE`). Three new broad patterns added: `have we ever`, `has X ever`, `what was our reasoning`. History router 0.81 / 0.81 → **1.00 / 1.00**. Three history guardrails removed from `PHASE_B_GUARDRAIL_TARGETS`.
- **Auto-routing recall pass — Phase B web landed.** `WEB_LOCAL_ONLY_RE` widened with project-ownership patterns ("we trained / built / ordered / ...", "across all three", "all three (cards|gpus)", "the (node|box|rack|...)", "container", "power draw", etc.). New fourth `detect_web_targets` branch routes search on explicit + fresh + no-local without requiring a public-object hit (catches proprietary part numbers). Web router 0.82 / 0.93 → **1.00 / 1.00**. One web guardrail removed; two remain (live g006/g007).
- **Auto-routing recall pass — Phase B live landed; Phase B complete.** `LIVE_OBJECT_RE` extended with `box`. New `LIVE_PUBLIC_DEST_RE` + `LIVE_LOCAL_HINT_RE`: live refuses when a public destination ("on github", "online") or web-explicit signal ("look up", "verify online") is present without a local hint. Health fallback split into "explicit health words always add" + "project-context tokens add only when no specific check has fired" -- "current gpu temperature on the node" returns just `['gpu']` not `['health', 'gpu']`. Live router 0.78 / 0.68 → **1.00 / 1.00**. Two live guardrails removed; **PHASE_B_GUARDRAIL_TARGETS is now empty.** All four routers at 1.00 / 1.00.
- **Broader operator approvals shipped.** `/live` now exposes a small Mutate-tier surface alongside the Observe-tier reads. Diagnostics (`/live ps`, `/live logs <vllm|open-webui|ollama>`, `/live journal ollama`, `/live inspect <vllm|open-webui>`) run immediately. Mutations (`/live restart vllm-server`, `/live restart open-webui`, `/live restart ollama`) queue a `live-mutation` approval row and only execute after `/approve <id>`. Ollama restart uses the exact validated argv `sudo -n /bin/systemctl restart ollama`. Hard guardrails: no arbitrary container names, no arbitrary journalctl units, no templated systemd units, no `--follow`, no shell composition, no invalid local target, no restart without `/approve`. Audit: `live_diag_executed`, `live_mutation_refused`, `live_mutation_queued`, `live_mutation_executed`, `live_mutation_blocked`. 12 operator-approval safety tests.
- **Fixed `/live journal ollama` truncation semantics.** The first live smoke proved `/live restart ollama` queued and executed, but `/live journal ollama` hid the restart event because `journalctl -n 200` emits chronological output and Nodechat truncated from the tail. Nodechat now preserves the newest tail for chronological log/journal commands (`/live logs ...`, `/live journal ollama`) and preserves the head for non-log diagnostics. Safety tests cover both behaviors.
- **Fixed Windows-local live mutation queueing.** `/live restart ollama` now refuses before queueing if Nodechat is running locally on Windows without `--live-ssh`, because the underlying argv is Linux-only (`sudo -n /bin/systemctl restart ollama`). Refusal prints `LIVE_MUTATION_REFUSED`, writes `live_mutation_refused`, and creates no approval row. SSH-targeted or homelab-local sessions still queue normally.
- **Live smoke validation of bc17ec4** (model profiles Phase 1) passed end-to-end against both vLLM (`192.168.1.198:8000`) and Ollama (`192.168.1.198:11434`). Disclosure line printed correctly on every reply, audit captured `model_dispatched` rows with profile/model/endpoint/latency/status (including the negative-path timeout case).
- **Model auto-routing (Phase 2) implemented.** `/model-mode auto|manual|fast|strong|deep` controls per-turn dispatch (default `auto`). Auto defaults to `fast`, lifts to `strong` on long prompts (>800 chars), code markers, analysis verbs, history-routing intent, or multi-file repo routing -- only if vLLM is reachable (cached `/models` probe, 60s TTL). `deep` never auto-selected. **Per-turn dispatch only**: session.profile/.model/.base_url stay unchanged unless user runs `/profile` or `/model`. Disclosure shows rationale on lift (`[model: strong <- auto-routed: long prompt; code markers]`) and on fallback (`[model: fast <- strong unavailable: vLLM probe failed (3012ms)]`). New audit event `auto_route_model` fires on lift/fallback. 10 new safety tests, including the no-silent-switch guardrail in manual mode.
- **Model remote profiles (Phase 3) implemented.** OpenAI and Anthropic profiles are env-gated and session-gated. They appear only when key+model env vars are set, require `/remote-models enable`, and dispatch only when explicitly selected through `/profile <remote>`, `/model <remote>`, or `/model-mode <remote>`. Auto model routing never chooses remote. `/costs` reports per-session estimated remote usage; `model_dispatched` records `remote`, `provider_kind`, estimated input/output tokens, and estimated cost.
- **Tests:** 90/90 passing; routing corpus remains 1.00 / 1.00 across history, repo, web, and live with 15/15 guardrails passing.
- **CFD feasibility scoped for later.** This machine can credibly reach "Level 4" CFD for Nodehome-scale rack/chassis airflow and thermal modeling if the work is treated as a validated engineering loop, not a button-click benchmark. Current 16-core EPYC + 128GB ECC + 2TB NVMe is enough for serious moderate CPU CFD. Adding the second 128GB RDIMM set (256GB total, all 8 channels populated) would improve mesh size, transient case headroom, retained timesteps, ParaView/post-processing, and memory bandwidth, but it will not automatically make solves fast. OpenFOAM-class solvers are CPU-first unless a GPU-native solver is deliberately selected; do not count the RTX 3090s as generic CFD acceleration.
- **Memory-architecture watch lane added.** New durable research note `docs/wiki/research/memory-architectures.md` logs delta-mem / arXiv 2605.12357 as a serving-layer memory watch item, not a Nodechat pivot. Current decision: Nodechat's AI History KB + auto-routed repo/web/live context remains the production memory path. Revisit only on independent reproduction, a 30B-class checkpoint, vLLM/SGLang/TGI/Ollama integration, or a real Nodechat context-length failure that RAG cannot solve.
- **Agent evaluation collapse watch lane added.** New durable research note `docs/wiki/research/agent-evaluation-collapse.md` logs Kabir's "The CTF Scene Is Dead" as a high-signal warning about open-scoreboard contamination from frontier agents and unequal token budgets. Current decision: use it as an evaluation-design warning for Nodechat/security capability work, not as a stack decision.
- **Inference-architecture watch lane added.** New durable research note `docs/wiki/research/inference-architectures.md` logs Orthrus / arXiv 2605.12825 as a lossless-parallel-decoding watch item. Current decision: watch only; no Nodehome serving change until native vLLM/SGLang support and a relevant larger checkpoint exist. Optional lab-only benchmark gates are now recorded: stop/restart `vllm-server`, avoid GPU2, use disposable Docker isolation for `trust_remote_code=True`, verify lossless correctness before speed, hard time-box dependency setup at 4 hours, and keep lab artifacts out of git.
- **SANA-WM watch/use-candidate added.** `docs/wiki/research/inference-architectures.md` now tracks NVIDIA/NVlabs SANA-WM / arXiv 2605.15178 as a potential future world/video-model workload: 2.6B controllable 720p minute-scale generation from image + prompt + 6-DoF camera trajectory. Current decision: no production stack change; lab-only use waits for official runnable weights/docs, disposable isolation, no private data mounts, no GPU2 while pigtail rule is active, and explicit recovery of vLLM/Ollama afterward. Treat the RTX 5090 + NVFP4 performance claim as a future hardware signal, not proof for the current RTX 3090 stack.
- **GenCAD watch lane added.** New durable research note `docs/wiki/research/cad-generation.md` logs GenCAD / arXiv 2409.16294 as an image-conditioned neural CAD generation watch item. Current decision: optional bounded lab smoke only; no production stack change. It likely runs on Nodehome with Docker, a single unrestricted 3090, and `xvfb-run`, but output should be treated as research-grade simple parametric starter geometry biased by DeepCAD-style training data, not production CAD or CFD geometry.
- **Streams Index sidecar design pinned, no code yet.** Single-user, LAN-only event/streams command center designed end-to-end (codex draft + Claude review). Aggregator-primary (streameast-class) per-provider scrapers with Cloudflare fetch-tier escalation (`plain` → `curl_cffi` → `cloudscraper` → headless Playwright last-resort) and config-driven canonical-domain rotation handled by two CLI commands (`streams-cli set-base-url` + `streams-cli invalidate-domain`); aggregator URLs always open externally via `window.open(..., 'noopener,noreferrer')` to a hardened browser profile, never server-side health-probed -- outcome signal is user-driven "did that work?" post-play prompt only. Authorized HLS branch (future HDHomeRun OTA + Jellyfin live-TV bridge + case-by-case free legal streams) plays in-app via hls.js. **Permanent architectural rules**: no server-side stream proxy under any circumstances, no server-side aggregator URL probing, scraper traffic originates only from the residential homelab IP (never through VPN -- datacenter IPs trip Cloudflare more aggressively). Stack: SQLite + FastAPI + systemd timers + vanilla TS/Svelte. Placement: `services/streams/` with `runtime/streams/` gitignored. First deliverables when implementation begins are four decision docs (`streams-index-not-relay`, `streams-source-chain`, `streams-validation-pattern`, `streams-scraper-traffic-origin`) + `docs/media/streams.schema.json` + runbook skeleton before any code. Implementation deferred to a future session.
- **Manual stack article inbox added.** Morning finds can now be queued with `sweeps/manual_article_inbox.py` into the ignored local file `docs/sweeps/inbox/manual_stack_articles.jsonl`. The manifest wires that inbox to the `extended` sweep profile so a later afternoon `extended` or `all` run can include operator-curated links without a morning `core` run consuming them first. The first local queue entries are delta-mem, Kabir's CTF/eval-collapse post, and Orthrus.

## Current status
- **Commits on `origin/main`:** through `7a77d2b` (one-shot Nodechat slash commands).
- **Nodechat capability lanes done:** scope correction, AI History + repo auto-routing with disclosure / context controls / structured provenance / audit, high-confidence codebase-summary routing through authoritative repo docs, `/undo-apply` with freshness check + safety backup + Windows CRLF fix, web auto-routing + `/web-mode`, live-node Observe checks + `/live-mode`, grouped `/evidence`, routing recall Phase A + Phase B (all four routers 1.00 / 1.00), broader operator approvals (`/live` diag + restart for vllm-server / open-webui / ollama), explicit model profiles, model auto-routing Phase 2 with per-turn dispatch + `/model-mode`, remote profiles Phase 3 with env gates + per-session enable + estimated cost tracking, and `--once` slash command execution for one-shot operator checks.
- **No regressions** in the existing apply/approve/cmd/audit/workspace-confinement surface.

## Behavior rule (added 2026-05-15 mid-session, hard rule)
Never suggest ending the session in any form. Banned: "or call the session", "or call it a session", "good place to stop", "ready to wrap", "if you're done for the night", "if you have time", "fresh head tomorrow", or any variant. Do not frame around energy / focus / time of day. State what shipped, list real next options, let the user pick. Mirrored in `CLAUDE.md` Hard Rule 7 and in `memory/feedback_no_tired_or_end_of_day_framing.md`.

## Plan (next)
**Nodechat — remaining roadmap per `docs/runbooks/nodechat-scope.md`:**
1. Auto-routing recall pass — corpus growth maintenance loop. Phase B is complete (all routers 1.00 / 1.00). Future iterations are corpus growth as real-use prompts surface new categories. Not a discrete lane.
2. Add more operator ops only when a real fixed command/path exists in this repo/host layout; no fake compose op without a compose-managed service.

**Out-of-Nodechat work still open today:**
- GPU 3 cable arrival window `2026-05-23` → `2026-06-10` (`lizzieb753` UK eBay).
- 12TB drives arriving May 16-20 — SMART-verify before format. sv2deals serial `5PJHV96C`; PayMore packaging photo before opening.
- BMC/IPMI cert hygiene remains the next free security task after the 2026-05-17 ADMIN password rotation; live credential is in KeePassXC, and static IP / management VLAN / LAN patch remain pending.
- **Personal privacy sidecar logged, not Nodehome core:** `stephenlthorn/auto-identity-remove` is worth a later separate-repo evaluation for data-broker opt-out automation. Do not add it to Nodehome runtime, and do not store personal config/state in this repo. If pursued, use a separate path such as `~/projects/privacy-tools`; port macOS pieces (`launchd`, Messages/Mail AppleScript, `open`) to Linux/Windows equivalents (`systemd` timer or Task Scheduler, local logs/SMTP/webhook summary). Hard caveat: upstream uses CapSolver, which can send CAPTCHA screenshots/HTML and broker-page context to a third party; using it means accepting a privacy leak around real name/address/email/phone/broker pages unless replaced. The real maintenance cost is ongoing selector/form repair as broker sites change.
- **Streams Index sidecar planned, not yet started.** End-state design pinned this session (see the "Streams Index sidecar design pinned" bullet above). When implementation begins: four decision docs + schema + runbook skeleton first, then SQLite catalog + migrations, FastAPI service skeleton, schedule providers (ESPN + per-league fallback chain), live grid frontend, click-outcome capture + counters, first aggregator scraper (streameast as the first because it forces the canonical-domain + Cloudflare-tier patterns correctly), then crackstreams/viprow/methstreams as additional pluggable scraper modules. HDHomeRun + Jellyfin live-TV bridge unlock the in-app `hls_authorized` playback branch when hardware lands (tracked as a trigger-gated Tier 4 row in `docs/runbooks/hardware-upgrade-roadmap.md`). Fully orthogonal to current Nodehome blockers (GPU3 cable, drives, BMC LAN patch). Implementation deferred to a future session.
- Historical `Co-Authored-By: Claude` attribution trailers: scrub executed 2026-05-18 after explicit user direction. Post-filter audit before this follow-up log commit was `174` commits, `0` trailer hits. Keep using `CLAUDE.md` Rule 9 / no AI attribution trailers for all future commits.
- Open WebUI re-pin `:main` -> `:v0.9.5` complete; next upgrades go through monthly release review.
- CFD proof path remains future work: OpenFOAM proof run -> simplified rack/chassis airflow model -> measured probes -> calibrate against idle/load thermal data -> increase mesh/detail. Buy/add the second 128GB RAM set when a sane matching RDIMM opportunity appears or when the proof path shows 128GB is the limiter.

## Pre-session work (carry-forward from Session 21 evening, still relevant)

Focus carryover: Storage procurement recovery after Walmart canceled Drive #1; replacement 12TB Easystore secured through eBay within the same market window; `whichllm` evaluated as a model-scouting tool.

### What was decided/executed in Session 21 (evening)
- **Walmart Drive #1 canceled:** order `2000146-01251834`, WD My Book 12TB (`WDBBGB0120HBK-Newm`), was canceled as out of stock after support had previously said the order would remain active and be fulfilled. Walmart email says temporary hold on VISA ending `3415` for `$269.54` should release within 10 business days.
- **Replacement Drive #1 secured:** WD Easystore 12TB (`WDBAMA0120HBK`) from eBay seller `sv2deals` (`499`, `100%` positive). Seller declined `$200` and `$210`; accepted `$220` best offer. Listed shipping was `$13.16`; final tax/all-in amount should be verified from order details once payment finishes processing.
- **Replacement SMART evidence is strong:** listing screenshot showed `WDC WD120EDAZ-11F3RA0`, serial `5PJHV96C`, 12 TB, USB, temp `29 C`, uptime `2 days, 6 hours`, `54` power-on hours, `15` power cycles, and zero reallocated / pending / uncorrectable / UDMA CRC errors. Treat as near-new usage but still used/no-retail-warranty until arrival verifies the same serial and SMART data.
- **PayMore Drive #2 still in flight:** WD My Book 12TB "new" from PayMore Westport (seller LLC: SPEEKS Technology, Overland Park KS), eBay order `03-14645-30973`, `$259.79` total with free shipping, delivery May 16-20 via USPS Ground.
- **AI History tool grounding generalized:** first Open WebUI tool test succeeded, but Qwen treated `GPU2` versus physical `GPU #3` as uncertainty. Updated the repo-owned Open WebUI tool to return a reusable `PROJECT_CONTEXT_CONTRACT` with evidence/provenance rules and canonical Nodehome aliases, rather than a one-off GPU2 note.
- **Market shock confirmed industry-wide.** Same SKU spiked to $323.49 hours after purchase. Multi-retailer check confirmed: WD sold out for 2026, prices up ~46% since Sept 2025, Best Buy 12TB Easystore sold out, Newegg $440. Root cause: AI-hyperscaler nearline HDD demand.
- **New durable doc:** `docs/wiki/concepts/hardware-supply-2026.md` capturing the market context, related DDR4 ECC RDIMM trajectory, decision rules for spending in this market.
- **Drive placement decision:** existing 1U cantilever shelf at U7 above the RM400. No purchase needed. Drives side-by-side, USB cables with slack loop for slide-out.
- **`whichllm` reviewed as a candidate scout, not a runtime change:** GitHub project `Andyyyy64/whichllm` auto-detects/simulates hardware, ranks local models from live HuggingFace + benchmark data, supports JSON output, `plan`, `run`, and snippets. It is useful for the model-watch / sweep lane, but should not replace validated local benchmark results. Do **not** run `whichllm run` or pipe its output into `ollama run` on the node while the GPU2 pigtail rule is active; use planning/JSON mode first.

## Validation backlog (re-ranked 2026-05-13 with safety edges tightened)
1. **Staged 2-GPU vLLM thermal soak** — 2026-05-14 status: complete/pass. No-top-fan stock-power run completed 180 iterations with GPU0 topping at `84 C` / `90% fan`; top-fan stock-power repeat completed 180 iterations with GPU0 at `77 C` / `82% fan`, GPU1 at `71 C` / `72% fan`, GPU2 isolated at `1 MiB` / `0% util`, and post-run healthcheck `[HEALTHY] no failures, no warnings`. `350 W + top fan` request baseline averaged about `4.96 s/request`; `300 W + top fan` averaged about `5.16 s/request` (~4% slower). The `300 W + top fan` 180-iteration soak completed cleanly with final loaded state GPU0 `76 C` / `78% fan` / `299 W`, GPU1 `70 C` / `69% fan` / `290 W`, GPU2 idle at `52 C` / `1 MiB` / `0% util` / `P8`, cooldown to `45 C` / `40 C` / `41 C`, and healthcheck `[HEALTHY] no failures, no warnings`. **Production 2-GPU vLLM default: top fan on, GPU0/GPU1 capped at `300 W`, GPU2 unused until the proper SF-1600F14HT cable arrives.** Follow-up: install a narrow persistent power-cap systemd unit only after explicit approval.
2. **Power consumption baseline** — Kill-A-Watt or smart plug. Capture multiple states: idle / vLLM loaded idle / 2-GPU inference under load / (later, post-cable) 3-GPU under load. Foundational data — informs UPS sizing (Tier 1 spend), circuit capacity, heat, noise. Higher-priority than I initially listed.
3. **Healthcheck automation** — **narrow** NOPASSWD entries only for specific read-only commands the script needs (not broad `sudo -n` access). Cleaner alternative: run as root via systemd timer instead of cron. Operational hygiene without broadening the attack surface.
4. **Network throughput** — 2026-05-14 practical SCP baseline complete. `iperf3` installed on `homelab` and server started, but Windows client had no local `iperf3`, so the measured baseline used OpenSSH/SCP over the same 1GbE path. Results: node -> Windows `88.5 MB/s` (~708 Mbps), Windows -> node `67.8 MB/s` (~542 Mbps) using a 1 GiB `/tmp/nettest.bin`; local and remote test files were cleaned up. Interpretation: path is functional but this is an encrypted file-copy baseline, not a clean NIC line-rate test. True `iperf3` remains optional if a Windows client binary is installed later.
5. **Release-notes review** (vLLM v0.21.x, Ollama v0.30.x) — **review BEFORE upgrading, not before proving current stack.** Current stable stack already works; release notes are upgrade-decision input, not validation work.
6. **RAM stress test** — lower urgency since the machine has booted and run on the RDIMMs already. If doing it: `stress-ng --vm 14 --vm-bytes 6G --timeout 4h` style — leave OS headroom (~30-40 GB), don't allocate the full 128 GB.
7. **NVMe non-destructive `fio` benchmark** — **CRITICAL: use `--filename=/path/to/testfile` against a file on the mounted filesystem, NOT raw `/dev/nvme0n1`.** Raw-device fio destroys the filesystem. Test file should be ~10-50 GB on root, removed after.
8. **Multi-model concurrent load** — lower value. Already know vLLM at 0.85 leaves ~2 GiB free, Ollama models won't fit. Test should validate **graceful failure** (Ollama returns OOM cleanly, doesn't crash the host or Open WebUI), not performance.
9. **Backup pipeline groundwork** — wait until external drives are physically present, unless just drafting scripts. Premature setup against non-existent mount points adds zero value.

**Gated on GPU 3 cable arrival** (sustained 3-GPU work, TP=3 vLLM, 70B AWQ across all 3, ReBAR A/B): wait for cable; window 2026-05-23 to 2026-06-10.

## Future hardware spend roadmap
- Captured in `docs/runbooks/hardware-upgrade-roadmap.md` — prioritized future hardware spends with concrete triggers.
- Current priority decision (updated after 2026-05-17 BMC rotation): GPU 3 proper cable remains the safety unlock; BMC/IPMI cert hygiene is the next free security task; RAM can jump ahead of UPS as the next spend if a clean matching 4x32GB RDIMM set appears at sane price; the cable-company router should be replaced rather than used as the long-term segmentation foundation.
- UPS posture revised: current local UPS is acceptable for blips and graceful shutdown at idle/light load. It is not sized for peak multi-GPU ride-through, and the stack should not plan to run sustained inference through an outage on the BX1500M.
- RAM posture revised: more RAM (4x 32GB DDR4-2400 ECC RDIMM, target Samsung M393A4K40CB1-CRC4Q) is not an emergency capacity gap today, but DDR4 ECC RDIMM has the highest run-away-price risk in the remaining upgrade list.
- CFD posture added: RAM matters more if rack/chassis CFD becomes real, but the first blockers are geometry cleanup, fan/heat assumptions, boundary conditions, instrumentation, and mesh-convergence validation. GPUs are not assumed to accelerate the first OpenFOAM-class path.
- Network segmentation posture: important before dedicated IPMI LAN patch or broader service exposure, but execute as phases: Phase 1 credentials/cert first, then replace the router/gateway path. The live ISP router is a Spectrum/Sagemcom `SAX1V1S` Advanced WiFi router; do not build VLAN/IPMI isolation around it. Preferred target stack: Spectrum modem -> Ubiquiti Cloud Gateway Ultra -> UniFi Switch Lite 8 PoE -> U7 Lite AP, bypassing/returning the Spectrum router. Cloud Gateway Max only if >1Gbps / 2.5GbE headroom becomes a real requirement.
- UPS upgrade remains a real reliability upgrade (used SMT2200, ~$200-350), but it no longer outranks a sane matching RAM opportunity.
- Tier 4 (deferred until specific trigger fires): storage expansion, NVMe expansion via Hyper M.2 card, 10GbE networking, PiKVM v4, internal CA.

## Open follow-ons
- **AI history KB long-termization:** repo utility `scripts/ai_history_kb.py` now owns the unified Claude/Codex/Claude Code history resource flow; runbook is `docs/runbooks/ai-history-knowledge-base.md`. First unified DB on node is `~/node-private/chat-exports/unified/index/ai-history-2026-05-14.sqlite` with 210,064 indexed items. Next integration step is Open WebUI tool/function wiring to the local HTTP API, not treating the DB as always-on model memory.
- **Memory architecture watch:** `docs/wiki/research/memory-architectures.md` tracks delta-mem and future model-internal memory systems. Watch only; do not churn Nodechat's AI History KB/RAG architecture unless a deployable runtime/checkpoint beats the current `strong` profile plus auto-routed context on real Nodehome memory questions with acceptable provenance.
- **Agent evaluation watch:** `docs/wiki/research/agent-evaluation-collapse.md` tracks the CTF/open-scoreboard collapse pattern. Use it when designing Nodechat capability evals: fixed model budgets, explicit human/AI/full-agent tracks, and durable tool/action provenance are required or the result is mostly an orchestration/spend benchmark.
- **Inference architecture watch:** `docs/wiki/research/inference-architectures.md` tracks Orthrus and future decoding/serving architectures. Orthrus is high-signal but not production-ready for Nodehome today: current public checkpoints are Qwen3 1.7B/4B/8B, native vLLM/SGLang support is still advertised as future work, and production tests must avoid GPU2 while the pigtail rule is active. Any local Orthrus run is lab-only evidence gathering: stop `vllm-server`, use disposable Docker, prove token-for-token correctness before throughput, and do not commit lab artifacts.
- **CAD generation watch:** `docs/wiki/research/cad-generation.md` tracks GenCAD and future image-to-parametric-CAD models. GenCAD is a paper artifact/toolbelt curiosity: optional lab smoke, disposable Docker, single unrestricted GPU, no private mounts, keep checkpoints/generated STLs/renders out of git, and do not treat output as production CAD or Nodehome CFD geometry.
- **Manual digest ingestion:** `sweeps/manual_article_inbox.py` appends operator-curated article rows to `docs/sweeps/inbox/manual_stack_articles.jsonl` (ignored runtime inbox). `sweeps/run_daily.py` preserves per-row lane/source/confidence/novelty/action/why overrides from local JSONL sources, and `sweeps/sources.json` includes the manual inbox only in the `extended` profile for afternoon/all runs.
- **`whichllm` follow-up:** optional future experiment is a read-only `model-watch` pass using planning/JSON output, e.g. simulate `RTX 3090` recommendations and compare against the already validated local speed table. Keep it out of production routing until its recommendations are checked against actual vLLM/Ollama behavior on this hardware.
- **Model variability/settings audit:** standardize and document generation settings across Open WebUI, Nodechat profiles, and sweep synthesis before using model-to-model comparisons as evidence. Track temperature, top_p/top_k, repeat/presence/frequency penalties, max tokens, context length, and seed/determinism support. Use deterministic settings for benchmarks/regression checks and separate conversational defaults for normal chat.
- **Network replacement follow-up:** separate Spectrum modem feeding the `SAX1V1S` router over ethernet is physically confirmed. Target UniFi Cloud Gateway Ultra + UniFi Switch Lite 8 PoE + U7 Lite AP (~$337 before tax/cables), connect the UniFi gateway WAN directly to the modem, power-cycle the modem so it learns the new WAN MAC, and remove/return the Spectrum router. Double NAT is acceptable only as a temporary transition state, not the final BMC management posture.
- **Replacement Drive #1 arrival protocol:** on the sv2deals Easystore, first confirm serial `5PJHV96C`, then run `smartctl -d sat -a /dev/sdX` before formatting. Accept only if the SMART story still matches the screenshot: roughly `54` hours, `15` power cycles, and zero reallocated / pending / uncorrectable / CRC errors. If clean: wipefs + ext4 + mount as one of the media drives.
- **Drive #2 arrival protocol:** PayMore WD My Book 12TB is still treated as "new sealed but grey-market/no MFG warranty." Photograph packaging seals before opening; plug in, run `smartctl -d sat -a /dev/sdX` BEFORE formatting; check Power-On Hours (<30 = genuinely new, >200 = wiped-used, file return). If clean: wipefs + ext4 + mount next to Drive #1.
- **SCW RAM return shipped 2026-05-13** (dropped off today). Tracking via eBay return label; awaiting carrier confirmation, then SCW receipt, then $454.65 refund processing (typically 2-5 business days after receipt).
- YellowChoo path dropped (declined $160 / expired; drive showed visible cosmetic damage scratches + chipped enclosure + no SMART data — backed out for cause).
- savsystems $30 parts-drive offer: still pending response, can let it ride.
- After both real drives in: spin up Jellyfin container, ingest first household media test content.
- **Open decision still on the docket: leave factual negative seller feedback for SCW.** Window closes ~2026-06-03 (60 days from 2026-04-04 purchase). Decision deferred — undecided. If yes, factual not emotional. Come back after refund clears.
- **Second rack fan research — circle back after TP=3 thermal data lands.** Researched 2026-05-14 during 300W soak. Existing top fan is AC-powered (wall-wart → DC → fan), plugs directly into rack PDU. **Recommended spec: AC Infinity MULTIFAN S5 (~$25)** — single 120mm + included AC adapter + 3-speed inline controller, designed for AV cabinet exhaust, matches existing fan's plug-and-play AC-PDU pattern. Avoid the chassis-fan path (be quiet! Silent Wings Pro 4 BL098 or Noctua NF-A12x25) — both are 4-pin PWM 12V DC and would require retrofit power delivery for our setup. **Pre-buy task:** identify existing top fan brand/model (photo of fan + wall wart label) — if it's already AC Infinity, just clone it. **Trigger to act:** GPU 0 plateau > 80°C during TP=3 sustained load. If TP=3 stays ≤ 80°C with current setup, no second fan needed.

## Budget state (updated 2026-05-14)
- Walmart WD My Book 12TB: canceled; `$269.54` temporary hold to release, net `$0` once settled.
- Replacement Drive #1 (sv2deals WD Easystore 12TB): `$220` accepted offer + `$13.16` listed shipping; final tax/all-in amount pending order details.
- Drive #2 (PayMore via eBay WD My Book 12TB): `$259.79` (with tax, free shipping).
- **Known subtotal before sv2deals tax: `$492.95`** (`$259.79 + $220 + $13.16`). Likely near or slightly above the original `$500` ceiling after tax, but still defensible in the current 12TB market given one unit is near-new SMART and the other has a return window.
- savsystems $30 experiment: still pending, not budget-counted
- Justification: market shock confirmed mid-purchase (WD sold out 2026, prices up ~46% since Sept 2025); two 12TB drives near the original $500 target is a defensible outcome in a market where one new 12TB drive is $300-440 retail at authorized channels. Original budget targeted a pre-shortage market that no longer exists.

## Carried forward from this morning (Session 18 core)
- Sweep pipeline follow-up (1 open, see `sweeps/PIPELINE_FOLLOWUPS.md`): reliable X social collection transport remains offline; `72f0117` only mitigated stale OpenRSS scheduled noise. Current repair path is Forward Email notification ingest through `xnotify@nodehome.ai`; setup is blocked on X follow/notification rate-limit cooldown and then real IMAP dry-run proof.
- vLLM blog signal recovered: TurboQuant KV-cache quantization post worth reading when convenient (directly applies to the `--gpu-memory-utilization 0.85` production posture).
- Ollama v0.30.0-rc15 + v0.23.3 stable both shipped; deferred to next monthly upgrade review per `docs/runbooks/upgrade-cadence.md`.
- Open WebUI rolling-tag `:main` is replaced with explicit `:v0.9.5`; `docs/runbooks/upgrade-cadence.md` now records the pin and post-migration backup.

## Carried forward from earlier sessions (Sessions 16-17, still relevant)
- Permanent in-chassis install complete; rack-installed on Tedgetal sliding shelf at U3-U6.
- All 3 GPUs at PCIe Gen 4 x16 under load; pigtail rule still enforced on GPU 2 via Ollama `CUDA_VISIBLE_DEVICES=0,1` until GPU 3 cable arrives (window 2026-05-23 to 2026-06-10).
- BMC fan threshold fix landed (`docs/runbooks/bmc-fan-thresholds.md`).
- Option C resolved (Open WebUI routes to both Ollama and vLLM; per-model system prompt grounds Qwen in local hardware).
- Home media server scope captured (`docs/runbooks/home-media-server.md`).
- IPMI hardening scope captured (`docs/runbooks/ipmi-hardening.md`) — 4 phases, 4 decisions blocking execution.
- Stack upgrade cadence policy captured (`docs/runbooks/upgrade-cadence.md`).
- Local-AI thesis external references (`docs/wiki/research/local-ai-thesis-external-references.md`).
- Hardware supply 2026 context (`docs/wiki/concepts/hardware-supply-2026.md`) — new this session.

## Operational lessons added this session
- **Tech prices are climbing in 2026, not falling.** Specifically: HDDs (AI-hyperscaler demand) and DDR4 ECC RDIMM (EOL). The "buy when needed" framing only safely applies if (a) the use case is concrete and (b) the category isn't in supply distress. For HDD/DDR4 ECC RDIMM with a real use case, prefer buy-now over wait-and-see.
- **Used 14TB market is now dominated by heavy-use datacenter pulls.** Watch SMART data carefully — Read Recovery Attempts is the leading indicator of failure on rotating media, more telling than reallocated sector count alone (because retries hide trouble until they stop working). 30k+ Read Recovery Attempts is a pass regardless of how clean the other counters look.
- **Real-time price spikes confirm supply distress, not just listing variance.** Same SKU jumping 30% intraday on the last in-stock unit is a market signal, not an outlier.

# Current State

**Last Updated:** 2026-05-15

## Active Work
- Hardware bring-up is now well past POST. `Supermicro H12SSL-i` BIOS `3.3` / build `03/28/2025` / CPLD `F0.A6.47`, BMC firmware `01.05.02`, IPMI Working, all four `Samsung M393A4K40CB1-CRC4Q` RDIMMs trained at `Total Memory: 128 GB`.
- **Ubuntu Server 26.04 LTS is installed on `BLK0` (the Acer Predator GM7 2TB NVMe) and boots cleanly from NVMe.** Kernel is `Linux 7.0.0-15-generic`, hostname `homelab`, OS partitions are `/boot/efi` (1 GiB FAT32) + `/` (1.86 TiB ext4). System is fully patched.
- **Network is operational on `eno2`** (Broadcom BCM5720, MAC `90:5a:08:7b:73:55`, DHCP `192.168.1.198/24`, dual-stack with IPv6). SSH reachable from the user's workstation; the IPMI KVM is no longer needed for day-to-day work. `eno1` is intentionally unused (no cable). The BMC USB virtual NIC `enxbe3af2b6059f` at `169.254.3.1/24` is harmless.
- **All three RTX 3090s are now installed and enumerated.** GPU #1 at `81:00.0` (CPU SLOT1, validated under sustained load), GPU #2 at `C1:00.0` (CPU SLOT3, validated under load with the Llama 3.3 70B Q4_K_M layer-split run), and GPU #3 at `C2:00.0` (next available CPU x16 slot, brought up under the temporary pigtail rule). All three are `NVIDIA GeForce RTX 3090` (GA102), driver `nvidia-driver-595-server-open` at runtime `595.58.03`, CUDA runtime `13.2`. All three negotiated PCIe `gen.max = 4`, `width.max = 16`, and GPU #1's link was confirmed to actually run at Gen 4 x16 under inference load (idle Gen 1 → Gen 4 ramp captured via `nvidia-smi` mid-flight). Power delivery on the 2-cable / one-head-per-dual-head-cable configuration held cleanly at `348 W` (essentially the 350 W TDP cap) for an 89% utilization sustained inference run on GPU #1.
- The bus IDs `C1:` and `C2:` being adjacent indicates GPU #2 and GPU #3 share a PCIe root complex / IOD. That is informational for later NUMA-aware tensor-parallel work; it does not affect the temporary pigtail rule or the bring-up itself.
- **GPU #3 is operating under the temporary pigtail rule** (`docs/wiki/decisions/temporary-pigtail-rule.md`) until the proper PCIe modular cable for the SF-1600F14HT arrives. Permitted use: BIOS / `lspci` / driver install / `nvidia-smi` / brief supervised low-load smoke tests. Not permitted: long inference, unattended operation, stress tests, benchmarks, or any sustained multi-GPU load. Touch-check on the pigtail and connectors at idle was clean (ambient).
- **Day-one inference path is working end-to-end on the 2-GPU configuration.** Ollama is installed and the systemd service is active. `qwen3:8b` (~5 GB weights, ~10.9 GiB resident on GPU 0 because the default `num_ctx=262144` allocates a large KV cache) ran a real CoT prompt and returned clean output. Multi-GPU detection is confirmed at the framework level — Ollama sees both 3090s as `compute=8.6` CUDA devices and pools 48 GB VRAM. `llama3.3:70b-instruct-q4_K_M` (~42 GB) was also pulled and exercised end-to-end across both GPUs in pipeline-parallel layer-split mode, producing a coherent 1500-word essay at the expected `~8-15 tok/s` ceiling for 70B Q4 layer-split on PCIe 4.0 x16 without NVLink.
- **Interactive tier validated and ranked.** A 3-way A/B benchmark across the three 30B-class candidates on a single 3090, same prompt, `--verbose` for runtime stats:
  - `mistral-small3.1:24b` — `eval rate 51.13 tok/s`, `prompt eval rate 1501.38 tok/s`, `load 6.72 s`. Smallest weights (~15 GB) and fastest output. **Default daily driver.**
  - `gemma3:27b` — `eval rate 39.91 tok/s`, `prompt eval rate 411.74 tok/s`, `load 28.25 s` (anomalously slow cold load; should be faster warm).
  - `qwen2.5:32b-instruct-q4_K_M` — `eval rate 39.21 tok/s`, `prompt eval rate 677.02 tok/s`, `load 8.02 s`.
  - All three confirmed to fit on a single 3090 with KV-cache headroom; all three skip PCIe boundary crossings entirely; all three are `~3-5×` faster than the 70B Q4 layer-split. Day-one tiering empirically confirmed: 8B-class for fast smoke tests, 24-32B-class as the interactive workhorse, 70B-class reserved for "willing to wait" deep-reasoning prompts.
- **Local model inventory on disk (~98 GB total):** `qwen3:8b` (5.2 GB), `mistral-small3.1:24b` (15 GB, **daily driver**), `gemma3:27b` (17 GB), `qwen2.5:32b-instruct-q4_K_M` (19 GB), `llama3.3:70b-instruct-q4_K_M` (42 GB). Plenty of headroom on the 1.86 TiB drive for additional models.
- **Open WebUI is running** in a Docker container (`ghcr.io/open-webui/open-webui:main`), exposed on port `3000`, pointed at the local Ollama via `OLLAMA_BASE_URL=http://host.docker.internal:11434`. To make Ollama reachable from the container, a systemd drop-in at `/etc/systemd/system/ollama.service.d/override.conf` was added that sets `OLLAMA_HOST=0.0.0.0:11434` (rather than the default localhost-only bind). Browser access works at `http://192.168.1.198:3000` and the model dropdown auto-populates with all installed models.
- **Docker + GPU passthrough stack operational.** `docker.io 29.1.3` from the Ubuntu 26.04 archive, plus `nvidia-container-toolkit 1.19.0` from NVIDIA's apt repo with `nvidia` registered as a Docker runtime via `nvidia-ctk runtime configure --runtime=docker`. Smoke-tested by running `nvidia/cuda:12.6.0-base-ubuntu24.04` inside a container — all 3 GPUs visible from inside the container with the same `nvidia-smi` output as the host.
- **vLLM is running and benchmarked.** `vllm/vllm-openai:v0.19.1` Docker container launched in detached mode bound to GPUs 0 and 1 only (respecting the temporary pigtail rule on GPU 2), serving `Qwen/Qwen2.5-32B-Instruct-AWQ` on TP=2 with `--gpu-memory-utilization 0.85` and `--max-model-len 8192`, using the optimized `awq_marlin` kernel and FlashAttention 2. The OpenAI-compatible API listens on port `8000`. **First benchmark: `59.13 tokens/s`** (795 completion tokens in 13.45 s wall clock) on the same 500-word essay prompt used for the Ollama A/B. That is `~50%` faster than the same-class Ollama GGUF Q4_K_M run at `39.21 tok/s`, and `~4-7×` faster than the Ollama 70B Q4 layer-split at `8-15 tok/s`. The AWQ-Marlin + tensor-parallel + chunked-prefill stack beats Ollama's GGUF + pipeline-parallel even with the PCIe-NCCL fallback that consumer Ampere requires (no P2P custom allreduce on RTX 3090). Repo's day-one stack pin (`vLLM v0.19.1` for multi-GPU production, Ollama for single-GPU interactive) is now validated empirically rather than aspirationally.
- **2026-05-14 watched 2-GPU vLLM validation passed; `300 W + top fan` is the production 2-GPU default.** After a reboot-era restart, `vllm-server` came up cleanly on GPUs 0 and 1 only (`22472 MiB` loaded per card) while GPU 2 stayed isolated at `1 MiB`. `scripts/healthcheck.sh` returned `[HEALTHY]`, a real OpenAI-compatible chat-completions request returned `choices`, and a 10-request watched loop using `/tmp/vllm-hit.sh` completed `10/10` with `HIT_OK`. Three 180-iteration soaks then defined the operating profile: stock `350 W` without top fan reached GPU0 `83-84 C` / `90% fan`, GPU1 `76-77 C`, GPU2 `56 C` idle; stock `350 W + top fan` reached GPU0 `77 C` / `82% fan`, GPU1 `71 C` / `72% fan`, GPU2 `52 C` idle; `300 W + top fan` reached GPU0 `76 C` / `78% fan` / `299 W`, GPU1 `70 C` / `69% fan` / `290 W`, GPU2 `52 C` / `1 MiB` / `0% util` / `P8`, then cooled to GPU0/GPU1/GPU2 `45 C` / `40 C` / `41 C` after ~6 minutes. `350 W + top fan` request timing averaged about `4.96 s/request`; `300 W + top fan` averaged about `5.16 s/request`, roughly a 4% wall-clock cost for materially better thermal/acoustic headroom. Post-run `./scripts/healthcheck.sh` returned `[HEALTHY] no failures, no warnings`. Current recommendation: keep the top fan on, cap GPU0/GPU1 at `300 W` for sustained 2-GPU vLLM, keep GPU2 unused until the proper SF-1600F14HT PCIe cable arrives, and do not describe the pigtail as load-validated because GPU2 remained idle throughout.
- **Boot-time `300 W` enforcement is installed.** `/etc/systemd/system/nvidia-power-cap.service` exists on `homelab`, is enabled, and was started successfully on 2026-05-14. `systemctl status nvidia-power-cap.service --no-pager` showed `active (exited)` with `ExecStart=/usr/bin/nvidia-smi -i 0,1 -pl 300` exiting `status=0/SUCCESS`; live verification immediately afterward showed GPU0/GPU1 at `300.00 W` power limits (`43 C` / `37 C` idle). GPU2 was not targeted. The NVIDIA persistence-mode warning is non-blocking because the power cap applied successfully. Boot-time behavior will be verified on the next natural reboot.
- **Practical network throughput baseline captured.** `iperf3` is installed on `homelab` and was started as a server on port `5201`, but the Windows client did not have `iperf3` available, so the measured baseline used finite OpenSSH/SCP transfers over the same `192.168.1.198` path. A 1 GiB `/tmp/nettest.bin` file transferred node -> Windows at `88.5 MB/s` (~708 Mbps) and Windows -> node at `67.8 MB/s` (~542 Mbps). Both local and remote test files were removed. This validates the path for practical SSH/file movement but should not be treated as a clean 1GbE line-rate measurement because SCP encryption/Windows OpenSSH overhead can cap throughput below raw link capacity.
- **Updated speed table for this hardware (validated):** Ollama+GGUF on a single 3090 — `qwen3:8b` ~60+ tok/s (smoke), `mistral-small3.1:24b` 51.13 tok/s, `gemma3:27b` 39.91 tok/s, `qwen2.5:32b-q4_K_M` 39.21 tok/s. Ollama+GGUF on 2 GPUs — `llama3.3:70b-q4_K_M` ~8-15 tok/s (pipeline-parallel, PCIe-bound). vLLM+AWQ on TP=2 — `Qwen2.5-32B-Instruct-AWQ` 59.13 tok/s. TP=3 + 70B-class AWQ benchmarks are deferred until GPU 3 is unrestricted; `Qwen/Qwen2.5-72B-Instruct-AWQ` is the planned next-tier model since it matches Llama 3.3 70B in size class but is publicly available with no HuggingFace gate.
- **Sweeps pipeline is now augmented by the local LLM.** `sweeps/llm_synthesize.py` reads the most recent operator brief from `docs/sweeps/operator/`, posts the content to a local OpenAI-compatible chat-completions endpoint (default: Ollama at `http://localhost:11434/v1/chat/completions` with `mistral-small3.1:24b`; `--endpoint` switches it to the vLLM container at port `8000`), and appends a `## Local LLM Synthesis` section with a "top 3 things that matter" digest, an explicit "Skip" line for noise, and a generation timestamp. Stdlib-only Python, idempotent on re-run, no external API keys. End-to-end tested on the `homelab` server against `mistral-small3.1:24b` for the 2026-05-09 brief: synthesis was produced in ~4 seconds for 917 chars of output, correctly identified the same `vLLM v0.20.2`, `llama.cpp Gemma4 NVFP4`, and `X/OpenRSS fetch failure` items the keyword classifier had already raised, and added a useful "Skip: Future Architecture (empty)" line. The repo's stated automated-research goal — sweeps running synthesis through the owned inference layer rather than an external paid API — is now validated end-to-end.
- The operator briefs themselves remain gitignored (`docs/sweeps/operator/*.md`) since they are daily-run artifacts, not versioned content. The synthesis script is committed; the briefs it operates on are produced fresh by the sweep pipeline. Long-term, the sweep pipeline will run on the server alongside the synthesis; today it still runs on the laptop and the briefs were `scp`'d over for the round-trip test.
- The same script was also re-run against the vLLM container at `http://localhost:8000/v1/chat/completions` using `Qwen/Qwen2.5-32B-Instruct-AWQ` for cross-validation. With `temperature=0.2` and a structured prompt over a structured brief, the two models converged: both produced 917 characters of nearly identical synthesis output, same top-3 items in the same order, same `Skip:` line. Operationally that means model choice for this synthesis workload is a runtime convenience question (Ollama is always-on as a systemd service; vLLM lives in a Docker container that has to be running), not a quality question. Day-to-day default: Ollama + Mistral 24B. When the vLLM container is up anyway for production multi-GPU work, route synthesis through it for free.
- **Private AI history knowledge base is now scoped as a first-class local resource.** Claude desktop/web, Codex, and Claude Code exports were indexed into a unified SQLite FTS database under `~/node-private/chat-exports/unified/index/ai-history-2026-05-14.sqlite` while preserving source provenance labels (`claude-desktop`, `codex`, `claude-code`). Verified counts on first build: `claude-desktop 6117`, `codex 88484`, `claude-code 115463`, total `210064`. The intended behavior is optional project/reference memory, not always-on model memory: history context should be retrieved only for prior-decision/project-state queries and omitted for general knowledge questions. Repo utility: `scripts/ai_history_kb.py`; runbook: `docs/runbooks/ai-history-knowledge-base.md`.
- **Nodechat is the local agentic terminal environment for Nodehome — early implementation in place, scope is full agentic.** Authoritative scope: `docs/runbooks/nodechat-scope.md` (combines model reasoning, AI History, repo state, live node state, command execution, filesystem operations, internet research, provenance, and durable audit; capability with evidence as the philosophy, risk-tiered execution as the control system). Operational usage: `docs/runbooks/nodechat-terminal.md`. Implementation today: streaming chat against the local OpenAI-compatible vLLM endpoint, saved sessions, `/paste`, `/status`; **auto-routing for AI History, repo files, web context, and live node status** (Observe tier, conservative day-one heuristics) — history routes on prior-decision phrasing (`what did we …`, `remind me`, `previously`, `history of …`, `prior decision/incident`, etc.); repo routes on concrete artifacts only (named files like `CURRENT_STATE`/`SESSION_LOG`/`CLAUDE.md`/`SCRATCH.md`/`ATTITUDE.md`, known runbook stems, and path tokens like `docs/...`/`scripts/...`/`sweeps/...`/`tests/...`/`memory/...`); vague topic phrases and bare filenames do not route; repo auto-routing reads at most two files per turn with the same workspace confinement and secret-path refusals as `/read`; routing failures never block the chat turn. **Disclosure line printed before every assistant reply that routed anything**, e.g. `[auto-routed: history(2403 chars, "what did we decide about gpu2") | repo(read docs/CURRENT_STATE.md)]`; skips and errors disclosed inline. **Context controls:** `/history-mode auto|manual|off`, `/repo-mode auto|manual|off`, `/web-mode auto|manual|off`, `/live-mode auto|manual|off` (defaults `auto`), `/evidence` groups active blocks by source with counts, char totals, reference summaries, and global indexes, `/forget [n|latest|all]` drops blocks. **Structured provenance on every block** (source: `auto-history`, `auto-repo`, `manual-read`, `manual-cmd`, etc.; provenance dict with paths/queries/exit codes/approval ids); legacy blocks render under `manual-legacy`. Manual context tools still available: `/history <query>`, `/pwd`, `/tree`, `/read`, `/search-files`, `/git-status`, `/web-search`, `/web-fetch`, `/web-open`, `/live`; web also auto-routes direct URLs and fresh-public-data prompts, and live status auto-routes through fixed Observe-tier checks. Single-file patch proposals (`/propose-edit`, `/diff`), approval-gated proposal application with backups (`/apply --check`, `/apply --confirm`), and safe proposal undo (`/undo-apply [n|latest] [--check]`), read-only command capture (`/cmd`), and a narrow `/approve <id>` queue for selected Git network/update commands (`git fetch`, `git fetch origin`, `git fetch --all`, `git fetch --prune`, `git fetch --prune origin`, `git pull --ff-only`, `git push`) with clean-tree preflight. Destructive, privileged, package-manager, arbitrary-network, and unknown commands are hard-blocked today and will move into a multi-step approval tier as the safety model matures. Hardening: workspace confinement, ambiguous repeated-hunk refusal in `/apply`, resolved executable paths + `approval_id` recorded in `COMMAND_OUTPUT`. Audit: significant tool events plus `auto_route_history`, `auto_route_repo`, `auto_route_web`, `auto_route_live`, `live_check_executed`, `live_diag_executed`, `live_mutation_queued`, `live_mutation_executed`, `live_mutation_blocked`, `live_mutation_refused`, `undo_apply_checked`, `undo_apply_confirmed`, and `undo_apply_refused` events appended to `%USERPROFILE%\.nodehome\nodechat\audit\nodechat-audit.jsonl`; `/audit [limit]` view. **56 stdlib safety tests passing** in `tests/test_nodechat_safety.py` (13 prior + 12 first auto-routing + 2 reliability regressions + 4 undo/apply-backup + 4 web-routing + 5 live-operator + 1 grouped-evidence + 4 routing-corpus + 11 operator-approvals). **Broader operator approvals — first iteration shipped:** `/live` now exposes a small Mutate-tier surface alongside the Observe-tier reads. Diagnostics (`/live ps`, `/live logs <vllm|open-webui|ollama>`, `/live journal ollama`, `/live inspect <vllm|open-webui>`) run immediately. Mutations (`/live restart vllm-server`, `/live restart open-webui`) queue an approval row of class `live-mutation` and only execute after `/approve <id>`. `/live restart ollama` is deferred until a NOPASSWD sudoers entry is installed. Hard guardrails: no arbitrary container names, no arbitrary journalctl units, no `--follow`, no shell composition, no restart without `/approve`. Full reference: `docs/runbooks/live-mutations.md`. **Auto-routing recall pass — Phase A + Phase B (history + web + live) shipped; Phase B complete on every router.** `tests/routing_corpus.py` is a 100-prompt labeled corpus + harness. History patterns split into TIGHT (project-bound by phrasing) and BROAD (require co-occurrence with `HISTORY_PROJECT_CONTEXT_RE`). Web `WEB_LOCAL_ONLY_RE` widened with project-ownership constructions; new fourth `detect_web_targets` branch routes search on explicit + fresh + no-local even without a public-object hit. Live: `LIVE_OBJECT_RE` extended with `box`; new `LIVE_PUBLIC_DEST_RE` + `LIVE_LOCAL_HINT_RE` make live refuse when a public destination ("on github", "online") or web-explicit signal ("look up", "verify online") is present without a local hint; the over-broad health fallback split into "explicit health words always add" + "project-context tokens add only when no specific check has fired". **All four routers at 1.00 / 1.00 precision/recall with zero guardrail failures.** Phase B target (0.95 / 0.95) exceeded. Floors pinned at 1.00 across all routers; `PHASE_B_GUARDRAIL_TARGETS` is empty. Future maintenance: corpus growth as real-use prompts surface new categories. Windows launchers: `scripts/windows/nodechat-tunnel.cmd` then `scripts/windows/nodechat.cmd`; token stays outside repo in `%USERPROFILE%\.nodehome\nodechat.env`; launcher sets the workspace to `C:\Users\bmoor\Local_AI`. Near-term implementation roadmap (per `nodechat-scope.md`): auto-routing recall pass Phase B (heuristic widening per router); broader operator approvals after rollback/evidence surfaces are stronger; model routing after the current single-model terminal loop remains stable.
- **Service persistence verified.** Both Docker containers (`vllm-server`, `open-webui`) are now configured with `--restart unless-stopped`. A full `sudo reboot` was performed and the entire stack auto-recovered: 3 GPUs back, Ollama service active, both containers `Up`, all APIs serving 200. vLLM took `~95 seconds` from boot to "Application startup complete" — that warmup window is currently the only operational gap (a synthesis request landing in that window would fail), filed as a follow-on (retry-with-backoff in the synthesis client, or a systemd readiness wait).
- **One-command operational health view.** `scripts/healthcheck.sh` reports GPU/storage/Ollama/Docker/API/BMC/kernel-error status in a single pass. Read-only, exits non-zero only on failures (warnings don't gate). Two known warnings on a healthy stack right now: BMC LAN port unpatched (gated on rack-day cable patch) and one historical `__warn_thunk` kernel line from the proprietary nvidia module's first load on Linux 7.0.
- **Out-of-band recovery is documented.** `docs/runbooks/ipmi-recovery.md` records the BMC firmware version, dedicated NIC MAC (`90:5a:08:7b:71:6d`, verified via `ipmitool lan print 1`), the recovered factory-default ADMIN password from `docs/archives/SESSION_LOG_2026-04.md:57`, web UI / `ipmitool` access procedures, the failure scenarios this addresses and explicitly does not, and a hardening note flagging that the factory password is in repo git history and must be rotated before the BMC is patched into any LAN.
- **IPMI hardening is scoped.** `docs/runbooks/ipmi-hardening.md` captures the proactive counterpart to the recovery runbook: four phases (password rotation + cert hygiene; management VLAN network plumbing; static IP + cable patch; optional internal CA). Phase 1 (password rotation + cert hygiene) can run independently and is the highest value-per-effort piece. The later network phases should use owned routing/switching gear rather than the ISP router, with the current preferred path documented in `docs/runbooks/hardware-upgrade-roadmap.md`.
- **Home media server is scoped and storage procurement is in progress.** `docs/runbooks/home-media-server.md` captures the architecture (direct-attached USB storage on the AI node, no second compute node), hardware spec (2x WD 12TB external desktop drives), software stack (Jellyfin via Docker container, NVENC transcoding via the 3x 3090s, network streaming to TV-side client apps), and order-of-operations for install day. The 3090 HDMI outputs are not used in this design — server stays headless, streaming happens over the home network to Jellyfin client apps on smart TVs / Roku / Apple TV / Fire Stick.
- **Hardware upgrade roadmap (future spend prioritization).** `docs/runbooks/hardware-upgrade-roadmap.md` captures the prioritized future hardware upgrade options with concrete trigger conditions for each. Current 2026-05-15 decision: GPU 3 proper cable remains the safety unlock; BMC/IPMI Phase 1 (password rotation + cert hygiene) is the next free security task; RAM can jump ahead of UPS if a clean matching 4x32GB RDIMM set appears at sane price; the cable-company router should be replaced rather than used as the long-term VLAN/security foundation. Preferred network path: ISP box in bridge/modem mode -> Ubiquiti Cloud Gateway Ultra -> UniFi Switch Lite 8 PoE -> U7 Lite AP; Cloud Gateway Max only if >1Gbps / 2.5GbE headroom becomes real. Current UPS is acceptable for blips and graceful shutdown at idle/light load, not peak-load ride-through. Companion to the software-side `upgrade-cadence.md`.
- **Stack upgrade cadence and version-pinning policy.** `docs/runbooks/upgrade-cadence.md` captures the monthly review process for Ollama, vLLM, and Open WebUI release notes, the policy of pinning to explicit versions rather than rolling tags, upgrade triggers between scheduled reviews, and the standard upgrade procedure. Open follow-up: re-pull Open WebUI from the current `:main` rolling tag to `:v0.9.5` (current stable). Two upstream items on the watch list: Ollama v0.30.0 (currently rc15, major version bump from pinned v0.23.2 — deserves real release-notes review before any upgrade; Ollama v0.23.3 patch release also shipped 2026-05-12, deferred to next monthly review) and vLLM v0.20.x / v0.21.x.
- **2026 hardware supply context.** `docs/wiki/concepts/hardware-supply-2026.md` documents the AI-hyperscaler-driven HDD shortage (WD publicly sold out for 2026, prices up ~46% since Sept 2025) and related DDR4 ECC RDIMM EOL trajectory. Both are climbing, not falling. The "buy when needed, prices drop" framing the project was operating under is wrong for these categories through end of 2026 minimum. Decision rule: lock in capacity now for resources with a *concrete* use case; still hold speculative spend on unused capacity.
- **Two 12TB media drives are now ordered, after Walmart canceled the original Drive #1.** Walmart order `2000146-01251834` for WD My Book 12TB (`WDBBGB0120HBK-Newm`) was canceled as out of stock; `$269.54` temporary hold to release. Replacement Drive #1 is a WD Easystore 12TB (`WDBAMA0120HBK`) from eBay seller `sv2deals`, accepted offer `$220` plus `$13.16` listed shipping; seller SMART screenshot showed internal `WDC WD120EDAZ-11F3RA0`, serial `5PJHV96C`, `54` power-on hours, `15` power cycles, and zero reallocated / pending / uncorrectable / UDMA CRC errors. Drive #2 remains the PayMore Westport WD My Book 12TB order `03-14645-30973` at `$259.79` total, delivery estimated May 16-20, return window through June 19, with grey-market/no-MFG-warranty caveats. Known subtotal before any sv2deals tax is `$492.95`; final all-in total depends on eBay tax. Placement: existing 1U cantilever shelf at U7 above the RM400 (no purchase needed). After both drives validate clean: spin up Jellyfin container, ingest first household media test content. See `docs/runbooks/home-media-server.md` for the full architecture.
- **Ollama is system-pinned to GPUs 0 and 1 only.** Discovered when `nvidia-smi` showed GPU 2 at `135 W / 6446 MiB` after an Open WebUI chat — the temporary pigtail rule prohibits sustained load on GPU 3 / `C2:00.0`, but Ollama's "least-loaded GPU" scheduler picked GPU 2 because vLLM had GPUs 0 and 1 memory-loaded. The rule was operator-enforced, not system-enforced; Ollama did not know the rule existed. Fixed by extending the existing systemd drop-in at `/etc/systemd/system/ollama.service.d/override.conf` to set `CUDA_VISIBLE_DEVICES=0,1` in addition to `OLLAMA_HOST=0.0.0.0:11434`. After `daemon-reload + restart`, Ollama only sees 2 GPUs and cannot schedule anything on GPU 2 even under user load. vLLM is already explicitly bound to `device=0,1` so it was already correct; this patches the Ollama side.
- **Trade-off resolved (Option C landed 2026-05-10, Session 16).** With vLLM running at `--gpu-memory-utilization 0.85` and Ollama pinned to GPUs 0+1, there is only ~2 GiB free per card after vLLM's workers and KV cache, so Ollama cannot load a model while vLLM is up. The fix: Open WebUI now has a second OpenAI-compatible Connection pointed at the vLLM container at `http://host.docker.internal:8000/v1` (key `local`, prefix `vllm`). Chat routes to vLLM directly when the user picks `vllm.Qwen/Qwen2.5-32B-Instruct-AWQ` from the model picker; the Ollama models stay listed for use whenever vLLM is intentionally stopped. Fallback policy if vLLM is unavailable: Option B (stop the vLLM container entirely so Ollama can use the full 24 GB per card). Option A (lower vLLM memory utilization to 0.55) was explicitly rejected — the higher KV-cache headroom is the whole reason to run vLLM. Side fix: Qwen claimed "hosted by Alibaba Cloud" out of the box (pretraining identity); grounded it via a per-model System Prompt in Open WebUI Workspace → Models pointing at this hardware.
- **GPU #3 is enumerated and validated at the link layer, but operating under the temporary pigtail rule.** The remaining work to retire the rule and unlock unrestricted sustained 3-GPU load is sourcing the proper PCIe modular cable for the `Super Flower SF-1600F14HT` (Leadex Titanium 1600W). Acceptable sources: eBay `"SF-1600F14HT" cable` or `"Super Flower Leadex Titanium" PCIe cable`, CableMod configurator with PSU set to Super Flower Leadex Titanium, or Super Flower USA distributor email. Do **not** substitute EVGA Supernova / Corsair Type 4 / Seasonic / generic "compatible" cables — Super Flower Leadex Titanium pinout is brand-specific and cross-brand mixing has well-documented fry incidents.
- A **temporary pigtail rule is allowed for GPU #3 only** while the proper cable is in transit, governed by `docs/wiki/decisions/temporary-pigtail-rule.md`. Permitted use is bounded to BIOS / `lspci` / driver install / `nvidia-smi` / brief supervised low-load smoke tests, with touch-checks on cable and connectors. **Not permitted** under the temporary rule: long inference, unattended operation, stress tests, benchmarks, or any sustained multi-GPU load. The rule retires the moment the proper cable is installed.
- Ollama install actually placed `v0.23.2` on disk (the install script always pulls latest stable); this is the previously-reviewed-clean release that earlier sweeps decided did not justify churning the pin pre-bring-up. The pin is therefore moved from `v0.21.2` to `v0.23.2` on 2026-05-09 to match what is actually running, rather than pretending the install matches a stale pin.
- Known BIOS tuning item still open: `Re-Size BAR Support` remains `[Disabled]` while `Above 4G Decoding` is `[Enabled]`. ReBAR is the natural pairing for 3x RTX 3090 inference and should be A/B tested as a deliberate change after the third GPU is online and a stable baseline benchmark exists, not flipped blind.
- **Permanent in-chassis install is complete.** Session 16 (2026-05-10) closed out the chassis-side wiring that had been deferred during bring-up: all three left-side internal drive cage sections were removed (build is NVMe-only, the cages were dead weight blocking cable routing and GPU access), the front-panel header (JF1) on the H12SSL-i was wired to the chassis FP leads, both chassis fans (front + rear) were connected to motherboard fan headers, and internal wiring was tidied for the permanent posture (FP wires routed against chassis edge, USB 3 cable kept clear of GPU/fan zone, fan leads secured, no signal wires across PCIe slot faces). The top GPU was removed during chassis work for access and reinstalled afterward; all 3 GPUs are back in their original slots. Host powered back on cleanly after the rebuild — first post-rebuild smoke test was a successful Open WebUI chat with `gemma3:27b` through the Ollama path. Outstanding documentation gap: the JF1 pinout used for the FP wiring was not captured into a repo runbook; should be added before the next chassis-open event so the pinout doesn't have to be re-derived.
- **Outstanding mechanical follow-on:** a few motherboard standoff screws still need to be installed to fully secure the H12SSL-i to the RM400 tray. The board is in position and stable but not 100% screwed down. Must be completed before the rack-mount + permanent location move so the board cannot flex against the standoffs during handling. Cheapest moment is whenever the chassis is opened next (e.g., for the JF1 pinout capture or the GPU 3 cable swap).
- **Final deployment phase remains: rack-mount + IPMI patch + permanent location move.** The chassis itself is now fully wired and serviceable as a unit, but the RM400 is still on a desk, not rail-mounted in the `SysRacks 24x24`. The dedicated IPMI ethernet port is also still not patched into the rack-side network — IPMI is currently reachable only via the host OS's BMC virtual NIC at `169.254.3.1/24`, which is fine operationally but not true out-of-band. Tedgetal 1U sliding shelf arriving 2026-05-10 unblocks the rack-mount step. Plan is to do rack-mount + IPMI patch + permanent location move in one pass **after the GPU 3 cable arrives and the temporary pigtail is retired**, so the box is only handled and cabled-into-rack once with the final component set. BMC hardening before the LAN patch (rotate factory password, replace self-signed cert, set static IP, ideally place the BMC on its own management VLAN) is a prerequisite, not an afterthought — the factory `SYZIFLTPAK` is already in repo git history.
- Hardware safety rule for this project remains explicit: no intentional pin shorting, no guessed header operations, and no undocumented power-control steps during bring-up. Only labeled connectors, documented headers, or approved vendor tools/adapters are allowed.
- This is the first full server build in this configuration. It is expected that some parts may not fit perfectly on the first attempt and that an extra cable, bracket, adapter, or replacement part may still be needed to finish cleanly.
- Sweep system is operational and now produces daily digests, weekly rollups, follow-up queue items, and assumption-pressure checks.
- Sweep workflow now also produces a generated daily operator brief (`docs/sweeps/operator/`) so triage does not require pasting digests into chat.
- The operator brief now has basic scaling guards: recommendation line, source/entity caps, and suppression of already-triaged rows.
- The sweep scheduler was repaired on 2026-04-27 after laptop-style task settings caused missed runs and `0x800710E0` refusals; tasks now have a real working directory and `StartWhenAvailable=true`.
- X/OpenRSS source health was also recovered on 2026-04-27 by auto-enabling OpenRSS fallback when no `X_BEARER_TOKEN` exists and clearing stale X-only quarantine state.
- Release-note review now lands on `Ollama v0.23.2` as the day-one install target, matching what the official `install.sh` actually places on disk. Earlier sweeps reviewed this release as clean; the pin update simply removes the gap between the intended and actual install state.
- `vLLM` is now deliberately pinned to `v0.19.1` instead of `v0.19.0`.
- The earlier intermediate releases (`Ollama 0.22.0`, `0.22.1-rc0`, `0.23.0`) were reviewed as not strictly necessary, but the eventual move to `0.23.2` on install day made the pin discussion moot.
- Current serving posture remains: Ollama first, vLLM second, direct `llama.cpp` benchmark/watch only.
- `Qwen3.6-35B-A3B` is now logged as a future vLLM benchmark candidate, not a day-one model target.
- `Qwen3.6-Max-Preview` is now logged as a hosted proprietary coding model to watch for future routing/escalation, not a local-node target.
- `Kimi K2.6` is now logged as a serious future model-eval candidate, but it is not practical on the current 3x3090 node and is not part of the day-one local stack.
- `llm-openrouter 0.6` is now logged as future routing/fallback tooling to watch, not part of the day-one local stack.
- `whichllm` (`Andyyyy64/whichllm`) is now logged as a future model-scouting / model-watch helper. Use it for planning or JSON recommendations only until reviewed in this environment; do not use `whichllm run` or auto-pipe recommendations into Ollama on `homelab` while GPU2 is still excluded by the temporary pigtail rule.
- Sakana's `Conductor` paper is now logged as a real future orchestration signal for Nodehome: relevant to a later "manager of models" workflow layer, but not to the immediate hardware build.
- Cloudflare's new agent provisioning flow is now logged as a real future "agent-operated infrastructure" signal for Nodehome: relevant to a later orchestration/product layer, but not to the immediate hardware build.
- SubQ is now logged as a hosted long-context architecture watch item. Early access was requested on 2026-05-05; status is pending review/waitlist.
- Railway changelog #0288 is now logged as a practical agent-guardrails signal: reversible destructive actions, safer shared machine identity, and explicit deployment controls matter more as agents become operators.
- Cloudflare's agent provisioning flow with Stripe Projects is now logged as a stronger future architecture signal: agents can discover services, authorize against real user identity, spend against scoped budgets, buy domains, and deploy to production without bespoke setup.
- `llama.cpp` `b9010` is now logged as a real multi-GPU CUDA watch item for this box: it fixes PCI bus ID de-dupe behavior that could ignore other GPUs or trigger OOM conditions.
- `Ollama v0.23.0` has now been reviewed at a high level and does not currently justify moving the pinned `v0.21.2` day-one target for the Linux 3x3090 stack.
- The May 4-5 sweeps did not materially change the stack posture. Recent `llama.cpp` CUDA / multi-GPU fixes remain watch items, but no new release signal justified moving pins or changing the serving order.
- The 2026-05-05 extended sweep should be treated as lower-confidence evidence due to heavy X/OpenRSS degradation (`21` social feed failures, including `19` timeouts).
- `Ollama v0.23.2` has now also been reviewed at a high level and still does not justify moving the pinned `v0.21.2` day-one target for the Linux 3x3090 stack.
- `vLLM v0.20.2` is now on the release-review queue, but the current evidence does not justify moving the pinned `v0.19.1` helper image before baseline hardware bring-up succeeds.
- The May 8-9 sweeps did not materially change the stack posture. Recent `llama.cpp` release churn and backend-path commits remain watch items, but no new release signal justified changing the serving order.
- The workflow/social lane is currently not operator-trustworthy enough to drive decisions on its own: irrelevant Simon/Bluesky chatter is still leaking into top signals and synthesis, so infra and release items are the only reliable parts of these digests for now.

## Component Status
| Component | Price (incl tax) | Status |
|-----------|-----------------|--------|
| 3x RTX 3090 Gigabyte Turbo | $3,442 | Purchased, eBay #227287677142. **All 3 installed and link-validated** (`81:00.0`, `C1:00.0`, `C2:00.0`, all at PCIe Gen 4 x16). GPU #1 also validated under sustained TDP load. GPU #2 validated under multi-GPU layer-split inference. GPU #3 currently operates under the temporary pigtail rule; sustained load on GPU #3 is gated on installation of the proper PCIe modular cable for the SF-1600F14HT. |
| EPYC 7302P + H12SSL-i v2.0 | $985 | Arrived 2026-04-07 |
| PSU 1600W Titanium | $241 | Purchased |
| RAM 128GB DDR4-2133 ECC | $454.65 | Purchased 2026-04-04 from SCW Computers (eBay), arrived as 32GB 4DRx4 LRDIMM (`M386A4G40DM0-CPB2Q`) instead of the listed RDIMM. **Resolved 2026-05-13 via eBay-forced refund** after escalating past auto-close through human review. eBay sent prepaid return label; LRDIMM kit returns to seller. Full $454.65 refund issued. Net cost: $0 (refunded). See Session 19 for the dispute escalation story; saved as a procedural memory at `feedback_ebay_dispute_escalation.md`. |
| RAM 128GB DDR4-2400 ECC RDIMM | $428.67 | Replacement set installed and validated: all four `Samsung M393A4K40CB1-CRC4Q` sticks train, BIOS reports `Total Memory: 128 GB`. Order #26-14569-05057. |
| SSD Acer GM7 2TB | $291 | Purchased |
| SilverStone RM400 chassis | ~$260 | Purchased |
| Noctua NH-U9 TR4-SP3 cooler | $161 | Purchased from kuaka02 (Ada) |
| Noctua NF-A12x25 PWM case fan | $0 | Have it (came with rack) |
| SysRacks 24x24 server rack | $75 | Purchased |

## Budget
- **Spent (incl tax):** ~$6,339
- **Over original $5,600 budget by:** ~$739 (cooler upgrade from Arctic $70 -> Noctua $161, rack, plus replacement RDIMM purchase)
- **All components purchased and primary stack hardware now validated through POST.** The previously-open LRDIMM mis-shipment is resolved as of 2026-05-13: eBay-forced full refund ($454.65) after escalating the dispute past the 30-day auto-close window through human review. LRDIMM kit being returned to seller via prepaid label.

## Blocking Issues
- The only remaining purchase is the one PCIe modular cable for the SF-1600F14HT to retire the temporary pigtail rule on GPU #3. Cable order `05-14624-65956` placed `2026-05-09` with eBay seller `lizzieb753` UK, landed cost `$49.85`. **Realistic arrival window: `2026-05-23 to 2026-06-10`** per the seller's posted estimate; UK Royal Mail / eBay International to US is realistically 2-4 weeks. Continuing to look for a faster legitimate source in parallel (CableMod Rush+Express in 5-10 days at ~$70 from a US warehouse, or r/hardwareswap local pickup); the eBay UK order is the floor. Until the cable arrives and is installed, GPU #3 stays out of sustained workload rotation.
- No software version blocker. Day-one targets are now pinned Ollama `v0.23.2` (matching what is actually installed) and `vLLM v0.19.1` (installed and running for the validated 2-GPU TP=2 path). Sustained TP=3 vLLM remains gated on retiring the GPU #3 temporary pigtail rule.
- Hardware bring-up is otherwise complete. POST, OS install, network, all 3 GPUs enumerated at Gen 4 x16, driver stack validated, multi-GPU inference proved on the 2-card configuration. What remains is software-stack work and final physical deployment.
- Reminder: fitment surprises or a late extra order would be normal for a first-time dense rack/GPU build and should be treated as part of the learning process, not as a project failure.
- Final physical deployment (rails, rack mount, cable management, dedicated IPMI patch, permanent location in the living room) is a real remaining phase, separate from software bring-up. It is intentionally deferred until the proper cable for GPU #3 is installed and the temporary pigtail is replaced, so the box is only handled and cabled into the rack once with the final component and cable set.

## Known Failures
None.
